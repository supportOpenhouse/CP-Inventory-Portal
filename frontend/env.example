# Copy this file to `.env` (gitignored) and use the real API base URL.
# Must be prefixed with VITE_ for Vite to expose it to the client.
VITE_API_BASE_URL=https://cp-inventory-portal.onrender.com/api
